# Sample prepare inventory
# Copy to inventory/prepare.ini and adjust ansible_user / password vars if needed

[all]
# Set the temporary login user before bootstrap (e.g. vendor-provided admin)
ansible_user=ubuntu
# Uncomment if you need to override ssh key / password for initial login
# ansible_password=changeme
# ansible_ssh_private_key_file=~/.ssh/id_rsa
